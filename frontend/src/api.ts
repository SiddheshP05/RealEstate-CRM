const API=(import.meta.env.VITE_API_URL||'http://localhost:8080').replace(/\/$/,'')+'/api';
async function request<T>(path:string,options:RequestInit={}):Promise<T>{const headers=new Headers(options.headers);const token=localStorage.getItem('crm_token');if(token)headers.set('Authorization',`Bearer ${token}`);let r:Response;try{r=await fetch(API+path,{...options,headers})}catch{throw Object.assign(new Error('Backend unavailable'),{status:0})}if(r.status===401){localStorage.removeItem('crm_token');window.dispatchEvent(new Event('crm:logout'));throw Object.assign(new Error('Unauthorized'),{status:401})}if(!r.ok)throw Object.assign(new Error('Request failed'),{status:r.status});return r.json();}
export function get<T>(path:string):Promise<T>{return request<T>(path);}
export function post<T>(path:string,body:unknown):Promise<T>{return request<T>(path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});}
